// import { createSelector } from "@ngrx/store";
// import { IAuthState } from "../models";

// // The state is the GLOBAL state inside NgRx, and we are just take a slice, only the auth!
// export const selectFeature = (state: { auth: IAuthState }) => state.auth;


// // Create selector, to select single property of our global state
// export const selectIsSubmitting = createSelector(
//     selectFeature,
//     state => state.isSubmitting
// )